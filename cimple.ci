program primes
{

    declare x,k,y;
	if(x > y){
		x := x - y;
	}
	else{
		y := y - x;	
	}
      function divides(in x, in y){
	     if (x == k){
                return 1;
		}
            else{
                return 0;
		}
	}

	while(x>1){
		print(x);
	}
}.